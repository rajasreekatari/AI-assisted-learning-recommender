apiVersion: v1
kind: Service
metadata:
  name: learning-recommender-backend
  labels:
    app: learning-recommender
spec:
  selector:
    app: learning-recommender
    tier: backend
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP

